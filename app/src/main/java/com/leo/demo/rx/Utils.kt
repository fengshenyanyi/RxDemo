package com.leo.demo.rx

import android.util.Log
import java.lang.Exception

const val TAG = "Demo"
fun logw(msg: String) {
    Log.w(TAG, msg)
}

fun logd(msg: String) {
    Log.d(TAG, msg)
}

fun loge(e: String) {
    Log.e(TAG, e)
}